/**
 * /secteurs/luxe/rh remains public as a status page.
 *
 * The branch is not client-ready. The page must keep an explicit demo / roadmap
 * posture because HR use cases can fall under high-risk AI Act categories.
 */
export default function Layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
